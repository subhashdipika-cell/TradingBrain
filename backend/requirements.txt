fastapi
uvicorn[standard]

pydantic
pydantic-settings
python-dotenv

sqlalchemy>=2.0
asyncpg
alembic

psycopg[binary]

python-multipart

httpx

# Live / forward testing (Dhan). Optional: only needed for live data + orders.
dhanhq

pytest